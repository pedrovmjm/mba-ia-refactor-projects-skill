def notify_order_created(order_id, user_id):
    print(f"Pedido {order_id} criado para usuario {user_id}")


def notify_order_status(order_id, status):
    print(f"Pedido {order_id} atualizado para {status}")
